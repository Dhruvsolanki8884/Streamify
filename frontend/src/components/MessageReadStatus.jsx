import { CheckCheck } from "lucide-react";

const MessageReadStatus = () => {
  return (
    <CheckCheck className="size-4 text-primary" strokeWidth={2.5} />
  );
};

export default MessageReadStatus;
